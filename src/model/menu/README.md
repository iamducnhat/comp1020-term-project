# Model Layer - Menu Package (Tree)

## Các class cần tạo:
- `MenuNode.java` - Node trong cây menu
- `MenuTree.java` - Cây menu tổng thể

## Data Structure:
- **Tree**: Root -> Categories (Coffee, Tea) -> Drinks -> Sizes
- Lookup: O(log n)
